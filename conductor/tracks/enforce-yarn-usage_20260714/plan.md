# Implementation Plan: Enforce Yarn as Package Manager

**Track ID:** enforce-yarn-usage_20260714
**Spec:** spec.md
**Created:** 2026-07-14
**Status:** [x] Completed

## Overview

The migration is purely a toolchain/infrastructure chore with no application code changes. It is broken into four short phases: (1) local Yarn bootstrap, (2) `package.json` engine constraints, (3) CI/CD workflow updates, and (4) documentation update. Each phase is independently verifiable and can be committed separately.

---

## Phase 1: Bootstrap Yarn Locally

Remove `package-lock.json` and generate `yarn.lock` on the local machine so that it is committed alongside the package changes.

### Tasks

- [x] Task 1.1: Verify Yarn Classic is available (`yarn --version`). If not, install globally with `npm install -g yarn`.
- [x] Task 1.2: Delete `node_modules/` and `package-lock.json` from the repository root.
- [x] Task 1.3: Run `yarn install` to generate `yarn.lock` and populate `node_modules/`.
- [x] Task 1.4: Confirm the import alias and resolution of every dependency is correct by running `yarn build` locally.

### Verification

- [ ] `yarn.lock` exists at the repository root and is non-empty.
- [ ] `package-lock.json` is absent (or gitignored).
- [ ] `yarn build` exits with code 0.
- [ ] `yarn test` exits with code 0.

---

## Phase 2: Constrain Package Manager in `package.json`

Prevent accidental npm usage with engine and packageManager fields.

### Tasks

- [x] Task 2.1: Add `"packageManager": "yarn@1.x.x"` field to `package.json` (replace `1.x.x` with the actual version from `yarn --version`).
- [x] Task 2.2: Add `"engines": { "node": ">=18", "yarn": ">=1.22.0" }` to `package.json`.
- [x] Task 2.3: Update the `deploy` script from `"npm run build && firebase deploy"` to `"yarn build && firebase deploy"`.
- [x] Task 2.4: Create `.npmrc` at the repository root with content `engine-strict=true` to block accidental `npm install`.

### Verification

- [ ] Running `npm install` in the project root produces an error about engine-strict.
- [ ] `yarn install` still succeeds cleanly.

---

## Phase 3: Update GitHub Actions Workflows

Switch all four CI/CD workflow files from npm commands to Yarn commands.

### Tasks

- [x] Task 3.1: Update `.github/workflows/ci.yml`:
  - Change `cache: 'npm'` â†’ `cache: 'yarn'` in the `actions/setup-node@v4` step.
  - Change `npm ci` â†’ `yarn install --frozen-lockfile`.
  - Change `npm run lint` â†’ `yarn lint`.
  - Change `npm run test` â†’ `yarn test`.
  - Change `npm run build` â†’ `yarn build`.

- [x] Task 3.2: Update `.github/workflows/cd.yml`:
  - Change `cache: 'npm'` â†’ `cache: 'yarn'` in the `actions/setup-node@v4` step.
  - Change `npm ci` â†’ `yarn install --frozen-lockfile`.
  - Change `npm run build` â†’ `yarn build`.

- [x] Task 3.3: Update `.github/workflows/firebase-hosting-merge.yml`:
  - Change the single run step `npm ci && npm run build` â†’ `yarn install --frozen-lockfile && yarn build`.
  - Add comment at top of file: `# NOTE: Do not overwrite via 'firebase init hosting'. Yarn commands are intentional.`

- [x] Task 3.4: Update `.github/workflows/firebase-hosting-pull-request.yml`:
  - Change `npm ci && npm run build` â†’ `yarn install --frozen-lockfile && yarn build`.
  - Add the same protective comment at the top.

### Verification

- [ ] All four workflow files contain no remaining `npm` references.
- [ ] On the next triggered PR or push, CI passes using yarn (or validated by dry-run inspection).

---

## Phase 4: Update Developer Documentation

Ensure `doc/guias/Technical_Roadmap_and_Setup.md` reflects Yarn commands so new contributors do not use npm.

### Tasks

- [x] Task 4.1: Replace `npm install` with `yarn install` in the **Prerequisites / Installation** section.
- [x] Task 4.2: Replace `npm run dev` â†’ `yarn dev`, `npm test` â†’ `yarn test`, `npm run build` â†’ `yarn build`, `npm run coverage` â†’ `yarn coverage`, `npm run deploy` â†’ `yarn deploy` in all code blocks under **Development**, **Testing**, and **Deployment** sections.
- [x] Task 4.3: Replace `npm install -g firebase-tools` â†’ `yarn global add firebase-tools` in the prerequisites list (or note both options).
- [x] Task 4.4: Add a short note under Prerequisites stating Yarn Classic (v1.22+) is the required package manager.

### Verification

- [ ] No `npm` command appears in `Technical_Roadmap_and_Setup.md` except where explicitly comparing to Yarn.

---

## Final Verification

- [ ] All acceptance criteria in `spec.md` are met.
- [ ] `yarn dev`, `yarn test`, `yarn build` pass locally with zero errors.
- [ ] `yarn.lock` is committed; `package-lock.json` is absent from the repo.
- [ ] Documentation updated.
- [ ] Ready for review / PR against `main`.

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
